Deployment notes

- This repo includes Dockerfiles for backend and frontend.
- For cloud deployment, build images and push to a registry, then deploy to your container platform.
- Optionally use Terraform or cloud-specific IaC to provision managed Postgres, Redis, MinIO, and container services.
